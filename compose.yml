services:
  nd_apps-shiny_serve:
    image: ${ND_APPS_IMAGE:-nd_apps-shiny_serve:local}
    restart: unless-stopped
    ports:
      - "12347:3838"
    logging:
      options:
        max-size: "10m"
        max-file: "3"
